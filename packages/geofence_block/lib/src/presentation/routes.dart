import 'package:go_router/go_router.dart';

/// The geofence_block module has no standalone screens — blocking is enforced
/// inline at the country picker and auth gate. It contributes no routes.
final geofenceRoutes = <RouteBase>[];
