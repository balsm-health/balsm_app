<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  TEMPLATE — fill in the four placeholders before running an iOS build task.
  See README.md; this file is tracked but skip-worktree, so your edits stay local.

  Ad-hoc (release-testing) export. Installs only on device UDIDs registered in
  the provisioning profile named below.

  signingStyle is manual and the profile is named explicitly so the export works
  from the CLI with no Apple ID signed into Xcode — automatic signing fails with
  "No Accounts / No profiles for <bundle id> were found" on a machine where
  nobody has opened Xcode's account settings.
-->
<plist version="1.0">
<dict>
  <key>method</key><string>release-testing</string>
  <key>teamID</key><string>YOUR_TEAM_ID</string>
  <key>signingStyle</key><string>manual</string>
  <key>signingCertificate</key><string>Apple Distribution</string>
  <key>provisioningProfiles</key>
  <dict>
    <key>YOUR_BUNDLE_ID</key><string>YOUR_ADHOC_PROFILE_NAME</string>
  </dict>
  <key>compileBitcode</key><false/>
  <key>uploadSymbols</key><false/>
  <key>destination</key><string>export</string>
</dict>
</plist>
