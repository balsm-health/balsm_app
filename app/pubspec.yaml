name: app
description: Balsm runnable shell
publish_to: none
version: 0.1.0+1
environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  core:
    path: ../packages/core
  auth:
    path: ../modules/auth
  disclosure:
    path: ../modules/disclosure
  profile:
    path: ../modules/profile
  emergency_card:
    path: ../modules/emergency_card
  medications:
    path: ../modules/medications
  self_report:
    path: ../modules/self_report
  sessions:
    path: ../modules/sessions
  account:
    path: ../modules/account
  deletion:
    path: ../modules/deletion
  geofence_block:
    path: ../modules/geofence_block
  flutter_riverpod: ^2.6.1
  go_router: ^14.0.0
  app_links: ^6.3.0
  url_strategy: ^0.3.0
  lucide_icons: ^0.257.0
  # Typed i18n codegen (same fork as core) — app-shell strings live in
  # patient_app/i18n/*.i69n.json, generated via build_runner.
  i69n:
    git:
      url: https://github.com/hossameldinmi/i69n
      ref: feature/remote-source
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10+1
  qr_flutter: ^4.1.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  geolocator: ^13.0.1
  dio: ^5.8.0
  flutter_secure_storage: ^9.2.4
  sensors_plus: ^7.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  mocktail: ^1.0.4
  # Used by repo-root test/ suites (golden, phi_leak_fuzz) run via the app package.
  golden_toolkit: ^0.15.0
  flutter_lints: ^4.0.0
  build_runner: ^2.4.13
dependency_overrides:
  # path_provider_foundation 2.6.0 migrated to package:objective_c + the
  # experimental native-assets build, which mislinks on iOS (Foundation not
  # linked → "Couldn't resolve OBJC_CLASS_$_NSArray ... symbol not found").
  # Pin to the last method-channel build to sidestep native assets.
  path_provider_foundation: 2.4.1
flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/body/
    - assets/brand/
